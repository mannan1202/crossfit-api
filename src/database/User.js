//const DB = require('./db.json');

const loginUser = (workoutId) => {
  try {
  } catch (error) {
    throw {
      status: error?.status || 500,
      message: error?.message || error,
    };
  }
};

const createNewUser = (newUser) => {
  try {
  } catch (error) {
    throw {
      status: error?.status || 500,
      message: error?.message || error,
    };
  }
};
